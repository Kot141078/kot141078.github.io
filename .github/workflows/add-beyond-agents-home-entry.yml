name: Homepage patch validation

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow execution
        run: echo "workflow-ok"

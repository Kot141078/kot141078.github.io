# Search Console Submission Plan V69

Contract: `DIARY_CURATED_START_PATH_REFRESH_WORLD_INTELLIGENCE_V69`

V69 creates no new public URL and changes no sitemap membership.

## Manual re-indexing

Request re-indexing for these two materially refreshed curated surfaces:

1. `https://ivankotov.eu/diary/`
2. `https://ivankotov.eu/diary/start-here/`

## No new request required

- `https://ivankotov.eu/world-intelligence/` already exists and does not require a new request merely because the Diary now links to it.
- Sitemap resubmission is optional; the root set remains 322 URLs with zero additions and zero removals.

Do not manually submit:

- World Intelligence or Diary image assets;
- `diary-start-here.json` or other Diary JSON;
- feed XML;
- noindex tag pages;
- machine-readable JSON endpoints.

# INTEGRATION PASS

## Why this mode was chosen

- AGL integration status: `ABSENT`
- Volume II integration status: `ABSENT`
- Public site is stable relative to the freeze baseline; there is no post-freeze public drift to clean up
- The current visual layer is acceptable enough that cleanup should not outrank missing corpus integration

## What to do next

1. Integrate AGL into the existing corpus structure rather than inventing a parallel mini-site.
2. Integrate Volume II into the existing book/source/publication surfaces rather than treating it as an orphan repo.
3. Keep the next pass surgical and content-structural.

Recommended targets for the next pass:

- AGL: `/l4/`, `/corpus-map/`, `/evidence/`, `/topics/`, `/ai-governance/`, `/library/`, `/downloads/`, `/publications/`
- Volume II: `/qubit-of-hope/`, `/library/`, `/downloads/`, `/publications/`, `/corpus-map/`, `/reading-path/`

## What not to do

- Do not run a standalone CSS redesign.
- Do not add broad SEO/metadata churn unrelated to AGL or Volume II.
- Do not create gratuitous new entry modes or duplicate inventory pages.
- Do not "clean up" wording across the whole site during the integration pass.

## Will the next pass touch public HTML pages?

Yes.

An actual integration pass will need controlled edits to public HTML pages and likely to selected machine-readable indexes. Those edits should remain tightly scoped to AGL and Volume II integration and should avoid unrelated visual or navigational churn.

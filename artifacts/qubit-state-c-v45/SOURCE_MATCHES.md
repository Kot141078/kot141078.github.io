# SOURCE_MATCHES

The following placements are based only on verified source content and public metadata.

| Source statement | Site placement |
| --- | --- |
| `c[q]` is a derived state of already existing `c` inside `c = a + b`. | `/qubit-state-c/`, `/c-a-plus-b/`, `/advanced-global-intelligence/`, `/corpus-map/` |
| `q = controlled non-collapse under uncertainty`. | `/qubit-state-c/`, `/publications/`, `/library/`, `/downloads/` |
| L4 supplies collapse pressure and bounds uncertainty by cost, time, energy, risk, trust, and irreversible consequence. | `/qubit-state-c/`, `/l4/`, `/reading-path/` |
| The document is not a quantum computer, quantum consciousness, or new ontological origin claim. | `/qubit-state-c/`, `/qubit-state-c.json` |
| Public source surfaces exist as Markdown, PDF, and DOI. | `/qubit-state-c/`, `/downloads/`, `/evidence/`, `/llms.txt`, `/sitemap.xml` |

No invented metadata was used. Checksums, file sizes, dates, author fields, DOI, resource type, and public URLs were taken from the local source mirror, GitHub content metadata, raw GitHub content, DOI/CSL JSON, and Zenodo API metadata.
